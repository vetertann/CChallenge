FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./
COPY src ./src

RUN pip install --no-cache-dir uv \
    && uv sync --frozen --extra car-agent

ENTRYPOINT [".venv/bin/python", "src/track_1_agent_under_test/server.py"]

