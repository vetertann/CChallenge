# Workspace Helpers And Regression Testset

Current best full train run:
`output/run_configs/20260626-225051__run_configs-coroutine_full_train_cerebras_gemini_1__train-trials1-baseall-hallall-disall__gpt-oss-120b.json`

Configuration:
- Agent: Cerebras `gpt-oss-120b`
- Skill: `car_domain_120b.md`
- Evaluator/user simulator: Gemini 2.5 Flash
- Trials: `1`

Current score: `122/129` (`94.6%`) raw.

Raw failures:
- Base: `base_48`, `base_74`, `base_86`, `base_96`
- Hallucination: `hallucination_92`
- Disambiguation: `disambiguation_24`, `disambiguation_55`

Factored score notes:
- `base_86` is the organizer-confirmed explicit-route-options policy judge contradiction.
- `disambiguation_55` passes after the user correction; raw failure is the initial unsupported location lookup counted as tool execution error.
- `hallucination_92` executed the expected AC/window/fan tool sequence and failed only by user-simulator `HALLUCINATION_ERROR`, so treat as likely simulator/judge sensitivity unless repeated target trials show a wording issue.

Previous 3-trial reference run:
`output/run_configs/20260624-204337__run_configs-coroutine_full_train_cerebras_gemini_3__train-trials3-baseall-hallall-disall__gpt-oss-120b.json`

Configuration:
- Agent: Cerebras `gpt-oss-120b`
- Skill: `car_domain_120b.md`
- Evaluator/user simulator: Gemini 2.5 Flash
- Trials: `3`

The score labels below include both the previous 3-trial reference and newer
target/full-run evidence. This file is for regression tracking: helper changes
should improve failing/flaky helper-sensitive tasks without regressing the solid
ones.

## Design Rule

Helpers should enforce tool-surface safety, normalize awkward outputs, preserve
grounded state, and apply unambiguous policy preconditions. They should not
override explicit user choices, stored preferences, or a completed model plan.

Current helper-risk pattern:
- Route narration must be grounded in the selected route: include the selected `name_via` when available, do not call a user-selected route "fastest" unless it is actually fastest, and only offer alternatives where policy requires it.
- A route selected by ID can still be narrated as fastest/shortest when the returned route aliases prove that ranking. The ID proves identity; it does not erase grounded route-ranking facts.
- Do not describe a user-selected route as fastest unless that is true for the selected route.
- Do not perform default side effects before a missing user parameter is resolved.
- Do not infer missing user parameters by matching words in the user message. Helpers may rely on explicit helper arguments, grounded tool state, stored facts, policy constants, or a clarification answer.
- Invalid helper or wrapper argument values are internal retry errors, not evaluator-facing limitations. The retry message should name the wrong value and the accepted values. Missing evaluator tools or live parameters remain direct capability limitations to the user.
- ID arguments must come from policy state, successful tool results, or persisted grounded entities. Helpers and wrappers must not construct plausible IDs from names or placeholders.
- Accepted exception: only via-road label preservation may compare current-turn text against already-fetched candidate labels for the same start/destination segment. This is not allowed to invent a missing value, choose among ambiguous candidates, default a side effect, or carry previous raw wording through a bare follow-up such as "continue". If a choice must survive a follow-up, it should be a recorded `select_route(...)` fact.
- Do not claim a multi-step plan is complete unless the whole required action chain succeeded.
- If the user explicitly names a POI from grounded search results, raw downstream calls must preserve that POI identity instead of drifting to a higher-power or first-listed alternative.

## Helper Registry

| Helper | Description |
| --- | --- |
| `handle_pending_confirmation()` | Resolves a stored confirmation after a helper asked for explicit user confirmation; executes stored calls only on clear yes/proceed. |
| `get_distance_by_soc_value(initial_state_of_charge, final_state_of_charge=0)` | Calls `get_distance_by_soc(...)` and normalizes dynamic distance output keys into stable `distance`, `unit`, `distance_km`, `raw_key`, and `raw_value`. |
| `get_navigation_state(detailed_information=True)` | Calls `get_current_navigation_state(...)` and normalizes active state, waypoint IDs, route IDs, detailed waypoints/routes, start, and destination. |
| `get_contact_details(contact_ids, required_fields=None, role=None)` | Calls `get_contact_information(...)` and normalizes contact records keyed by ID, including nested name aliases. Optional explicit roles such as `email_recipient` and `contact_details_subject` are stored for email-recipient guards; repair only uses a current-turn role pair or a current/immediately previous-turn pair, never old stale roles. |
| `send_contact_details_to_contact(recipient_contact_id, subject_contact_id, required_fields=None, intro=None)` | Role-safe contact email helper: keeps recipient and subject contact IDs separate, reads recipient email and subject fields, builds a grounded details message, and routes through normal `send_email` confirmation. |
| `get_next_calendar_entry()` | Reads today's calendar and returns normalized entries plus the next entry at or after `policy_now()`. |
| `resolve_calendar_attendee_recipients(topic=None, start_hour=None, start_minute=None, location=None, month=None, day=None)` | For email-to-meeting-attendees requests, reads the calendar, resolves one meeting from structured selectors, requires concrete attendee contact IDs, then reads attendee emails. If attendee identities are unavailable, it reports that limitation directly instead of asking the user to invent them. |
| `defrost_front_window()` | Applies front-defrost policy: reads climate/window state, closes required windows, turns on required climate settings, and reports missing capabilities. |
| `set_window_defrost_safe(defrost_window='FRONT')` | Safe defrost wrapper for front/all/rear defrost; front/all applies policy 010/011 preconditions. |
| `open_sunroof_safe(percentage, target_is_explicit=False)` | Sets sunroof under sunshade and weather policies, including confirmation for unsafe weather. Full-open targets require `target_is_explicit=True`, which must come from a resolved argument, not user-message keyword parsing. |
| `open_close_window_safe(window, percentage, target_is_explicit=False)` | Moves a window under policy 007; asks confirmation for opening above 25% when AC is on or AC state is unavailable. Full-open targets require prior helper clarification state; a model-supplied `target_is_explicit=True` is not enough. |
| `set_fog_lights_on_safe()` | Activates fog lights under weather/exterior-light policies; handles low/high beam preconditions and confirmation. |
| `set_high_beams_on_safe()` | Activates high beams under policy 014; blocks when fog lights are on or fog-light status is unavailable, and routes known-safe activation through confirmation. |
| `set_exterior_lights_safe(intent)` | Handles broad exterior-light intents after the model resolves one of `improve_visibility`, `turn_on_headlights`, or `turn_off_exterior_lights`; reads exterior-light/weather state and delegates to policy-safe light actions without parsing raw user text. |
| `present_climate_comfort_options(intent)` | Response-only helper for broad comfort intents (`too_warm`, `stuffy_air`, `warm_up`); reads current climate settings when available, includes grounded fan/AC/circulation/airflow state in the clarification, and performs no setter side effects. The `warm_up` branch asks for both cabin temperature and seat-heating level when both are unresolved. |
| `set_air_conditioning_on_safe(use_preferred_air_circulation=False)` | Turns AC on under policy 011; closes windows over 20%, handles unknown controllable windows, sets fan speed to 1 when needed. The stored air-circulation preference is applied only when the model passes the explicit flag; the helper does not infer it from raw user text. |
| `close_known_windows_for_blocked_ac(window=None)` | Follow-up helper for blocked AC/defrost flows; closes only windows already known open from the previous helper report. |
| `set_climate_temperature_safe(seat_zone, temperature)` | Sets explicit temperature and reports driver/passenger differences above 3 degrees Celsius for single-zone changes. |
| `sync_climate_zone(source_zone, target_zone, include_temperature=True, include_seat_heating=True)` | Copies temperature and/or seat heating from one front zone to the other by reading state first. |
| `increase_fan_speed(steps=1)` | Reads current fan speed, increases it by the requested relative step count, clamps to supported range, then calls `set_fan_speed`. |
| `decrease_fan_speed(steps=1)` | Reads current fan speed, decreases it by the requested relative step count, clamps to supported range, then calls `set_fan_speed`. |
| `set_occupied_seat_heating(level=None, increase_by=None, seat_zone=None)` | Reads front-seat occupancy and current seat heating when needed, then sets occupied front seats only. If `seat_zone` is explicitly supplied, narrows to that front zone instead of expanding to all occupied seats. |
| `turn_off_unoccupied_seat_heating()` | Reads front-seat occupancy and current seat heating, then sets only unoccupied heatable front seats to level 0. If the current level for an unoccupied front seat is unavailable, it still sets that seat to 0 to make sure it is off. |
| `optimize_seat_heating_by_occupancy(occupied_level=None, unoccupied_level=None, include_rear=True)` | Reads occupancy and current front-seat heating when available, computes one explicit final level per heatable front zone, and calls `set_seat_heating(...)` at most once per front zone. It reports occupied rear seats as unsupported for seat heating instead of inventing rear controls. |
| `set_occupied_reading_lights(on=True, include_rear=True)` | Reads seat occupancy and calls `set_reading_light(...)` once for each occupied canonical reading-light position; rear aliases are normalized to DRIVER_REAR/PASSENGER_REAR without duplicate left/right calls. |
| `set_reading_lights_by_occupancy(occupied_on=True, unoccupied_on=False, include_rear=True)` | Reads seat occupancy and current reading-light state when available, computes one final desired state per canonical reading-light position, and calls `set_reading_light(...)` at most once per position. Use this when occupied and unoccupied seats need different final light states. |
| `get_route_options(start_id, destination_id)` | Calls route lookup and normalizes route IDs, aliases, duration, distance, toll metadata, fastest/shortest route IDs, and display strings. |
| `select_route(routes, route_id=None, alias=None, name_via=None, prefer=None, record_selection=True)` | Pure route selector; succeeds only when route ID, alias, name-via, or preference uniquely identifies one route. |
| `select_route_by_user_preferences(routes, preference_text=None, record_selection=True)` | Applies stored route preferences such as fastest, shortest, no toll, and no-toll-within-N-minutes-of-fastest. |
| `select_poi(pois=None, poi_id=None, name=None, category=None, record_selection=True, role=None)` | Pure POI selector; succeeds only when a POI ID/navigation ID/name uniquely identifies one POI. Optional `role` stores `selected_<role>_poi` so later steps can preserve charging-stop, meal-stop, destination, or companion identities without relying only on `selected_poi`. |
| explicit POI identity guard | Runtime raw-call repair: when a selected charging POI is already grounded, downstream charging-time and route-to-stop calls can be repaired to that selected POI only when the mistaken argument is not a different known POI. It does not parse the current user text. |
| `get_weather_at_route_arrival(location_or_poi_id, route=None, route_id=None, routes=None, start_id=None)` | Computes route arrival time and calls weather for the destination at that arrival time. |
| `navigate_by_arrival_weather(primary_destination_id, fallback_destination_id, avoid_conditions, route_prefer=None, start_id=None)` | Preferred short helper for primary/fallback weather navigation. Selects the primary route, checks primary arrival-time weather, then sets navigation to the primary or fallback route using model-supplied blocked conditions and route preference. If no grounded route preference exists and several branch routes remain, returns `ROUTE_SELECTION_REQUIRED` with a branch-specific route-choice message instead of defaulting to fastest. |
| `navigate_to_poi_by_arrival_weather(primary_location_id, fallback_destination_id, category_poi, avoid_conditions, poi_prefer='fastest_charging', route_prefer=None, start_id=None)` | Preferred helper when the primary branch is a POI category inside a primary location. Checks primary-location arrival weather; if blocked, sets fallback navigation; if clear, searches/selects the requested POI category and sets navigation to that POI using model-supplied POI and route preferences. If no grounded route preference exists and several branch routes remain, returns `ROUTE_SELECTION_REQUIRED` with a branch-specific route-choice message instead of defaulting to fastest. |
| `navigate_to_poi_unless_arrival_weather(primary_location_id, fallback_destination_id, category_poi, avoid_conditions, poi_prefer='fastest_charging', route_prefer=None, start_id=None)` | Short alias for `navigate_to_poi_by_arrival_weather(...)`; same implementation and contract, added to improve helper selection for "POI unless arrival weather blocks it" requests. |
| `set_navigation_conditioned_on_arrival_weather(primary_destination_id, fallback_destination_id, avoid_conditions, route_prefer=None, start_id=None)` | Descriptive long alias for `navigate_by_arrival_weather(...)`; same protocol and implementation. |
| `select_poi_at_location_open_at_route_arrival(location_id, category_poi, route=None, route_id=None, routes=None, start_id=None, record_selection=True)` | Computes arrival time, searches POIs at a location, parses opening hours, and selects the unique POI open at arrival. |
| `select_charging_plug(pois=None, require_available=False)` | Keeps charging station, phone, plug ID, power, and availability together; selects the highest-power plug and persists the selected station as the current selected charging POI. |
| `find_charging_stop_on_active_route_by_soc(reserve_state_of_charge, route_id=None, require_available=False)` | For active-route charging requests at a resolved reserve SOC, reads active navigation, charging status, official distance-by-SOC facts, converts current-location distance into the correct active route segment, calls `search_poi_along_the_route`, and stores selected charger/provider facts. If `require_available=True` is explicit and the live schema supports `filters`, the route search includes `charging_stations::has_available_plug`. It does not inspect raw user text. |
| `search_charging_stations_on_route(route_id, at_kilometer, require_available=False)` | For charging searches along a grounded planned route that is not active navigation, reads charging status when available and not already grounded, calls `search_poi_along_the_route` without starting navigation, and stores charger/plug facts. If `require_available=True` is explicit and the live schema supports `filters`, the route search includes `charging_stations::has_available_plug`. It does not infer route id or kilometer from user text. |
| `search_charging_stations_on_active_route(at_kilometer, route_id=None, require_available=False)` | For active-trip charger searches at a resolved route kilometer, reads active navigation, defaults to the first active route segment, calls `search_poi_along_the_route`, and stores charger/plug facts. If `require_available=True` is explicit and the live schema supports `filters`, the route search includes `charging_stations::has_available_plug`. It does not infer the kilometer from user text. |
| `estimate_charging_stops_for_route_by_soc_window(destination_id, charge_from_state_of_charge, charge_to_state_of_charge, start_id=None, route_prefer=None)` | For route charging-stop counts over a resolved SOC window, calls route lookup and official `get_distance_by_soc(upper, lower)`, then returns route distance, SOC-window range, and a ceiling-based stop estimate. It does not infer SOC values or route preference from user text. |
| `set_navigation_via_route_stop_with_open_poi(destination_id, stop_category_poi, companion_category_poi, window_start_hour, window_start_minute, window_end_hour, window_end_minute, start_id=None, route_prefer='fastest', candidate_kilometers=None)` | For navigation through an intermediate route stop where another POI category must be open at the same route position during a resolved time window. It derives route-kilometer buckets from route duration/distance and policy time unless explicit buckets are supplied, searches both categories along the route, pairs same-position POIs, checks opening hours, and sets two-leg navigation through the stop. The report explicitly marks the stop as the navigation waypoint and the companion as a non-waypoint POI open at the same route position. It does not infer categories, destination, or clock window from user text. |
| `set_new_navigation_via_stop(stop_id, final_destination_id, route_to_stop_prefer='fastest', route_to_final_alias=None, route_to_final_prefer='fastest')` | Builds a two-leg route through one stop and calls guarded `set_new_navigation(...)`. |
| `plan_charging_for_next_meeting(range_buffer_km=40, arrival_buffer_minutes=5)` | Plans minimum and maximum charging time before the next meeting using calendar, route, charging state, charger search, timing, and executable current->charger->meeting route IDs. |
| `call_selected_charging_provider()` | Calls the phone number for the currently selected charging provider/station. |
| `get_preferred_ambient_light_color()` | Reads vehicle preferences and extracts a unique valid ambient-light color. |
| `set_new_navigation_guarded(...)` | Guarded wrapper for `set_new_navigation(...)`; validates navigation shape and completion claims. |
| `get_routes_guarded(...)` | Guarded wrapper for route lookup; normalizes route outputs and unknown route-list failures. |
| `get_weather_guarded(...)` | Guarded wrapper for weather lookup; clamps month/day to policy today, preserves explicit hour/minute, hoists active-slot fields such as `condition` and `temperature_c`, and stores `last_weather`. |
| `search_poi_along_route_guarded(...)` | Guarded wrapper for route-based POI search. |
| `navigation_add_one_waypoint_guarded(...)` | Guarded wrapper for adding a waypoint; resolves route dependencies and duplicate no-ops. |
| `navigation_delete_waypoint_guarded(...)` | Guarded wrapper for deleting a waypoint; preserves a supplied grounded connecting route, otherwise derives the policy-default fastest replacement route, and treats already-removed waypoints as no-op success. |
| `navigation_replace_one_waypoint_guarded(...)` | Guarded wrapper for replacing one waypoint; validates route dependencies. |
| `navigation_replace_final_destination_guarded(...)` | Guarded wrapper for final-destination replacement; validates route dependencies and missing capability behavior. |
| `get_contact_id_by_contact_name_guarded(..., constrain_to_recent_calendar_attendees=False)` | Guarded contact lookup wrapper; normalizes contact IDs, exposes contact-query intersections, ranks a unique recent calendar attendee match first, can explicitly constrain same-name lookups to recent attendee IDs, repairs known POI/contact ambiguity where possible, and blocks unsupported arguments. |

## Helper Regression Testset

| Task | Score | Helpers or helper area | Regression covered |
| --- | --- | --- | --- |
| `base_0` | `3/3` | `open_sunroof_safe`, `handle_pending_confirmation` | Sunroof weather confirmation and sunshade precondition. |
| `base_20` | `3/3` | `get_next_calendar_entry`, calendar normalization | Calendar time formatting and normalized meeting fields. |
| `hallucination_59` | target-fixed `1/1` | `resolve_calendar_attendee_recipients`, calendar attendee normalization | Email-to-meeting-attendees flow stops when the calendar entry's attendee identities are unavailable; latest target run found the meeting and gave the direct limitation without contact lookup or email sending. |
| `base_28` | `3/3` | `increase_fan_speed` | Relative fan-speed change must read current climate state first. |
| `base_48` | latest full raw fail, action/tool/final pass | `navigation_replace_final_destination_guarded`, route narration | Latest miss is policy-only after the agent showed alternatives before a user-selected non-default route. Do not pre-commit fastest before user route choice; do not narrate selected routes as fastest unless grounded. |
| `base_54` | latest target pass; latest full pass | `set_climate_temperature_safe`, `set_occupied_seat_heating`, temperature-unit response repair | Climate and occupied-seat heating; response must say degrees Celsius after successful temperature setters. |
| `base_56` | latest full pass | `navigation_delete_waypoint_guarded`, route narration | Correct waypoint deletion should not be followed by route-option invitations that keep the conversation alive. Latest full run passed. |
| `base_64` | `3/3` | `navigation_add_one_waypoint_guarded`, `get_navigation_state` | Mid-route insertion dependencies and route-structure handling. |
| `base_70` | `3/3` | `get_distance_by_soc_value`, charging state normalization | EV range/charging answer must use charging specs before answering. |
| `base_74` | target-fixed `3/3`; latest touched-helper slice pass | `handle_pending_confirmation`, route/charging/email planning, `get_distance_by_soc` normalization, charging email detail repair, range-insufficiency pre-send guard | Route/travel emails preserve grounded selected charging-plan details once those facts exist, block incomplete charging-need claims, and now also block route/travel drafts that omit a known range insufficiency when route distance exceeds current range and no charging plan exists. The guard does not choose current-location or en-route charging from raw user wording. |
| `base_76` | target `3/3`; latest full pass | `sync_climate_zone` | Copy passenger values to driver, not the inverse. Prompt examples reduce wrong-direction calls. Do not add helper-side repair that infers intent from opposite-direction calls. |
| `base_78` | `3/3` | `get_contact_details`, confirmation recipient repair | Contact-set intersection and email recipient grounding. |
| `base_82` | latest full pass | `select_route`, `navigation_replace_final_destination_guarded`, route narration | Explicit user route via `K57, B65` must not be overridden or narrated as fastest. Latest full run passed. |
| `base_84` | latest full pass | `select_poi`, `select_charging_plug`, `set_new_navigation_guarded` | Multi-leg navigation via charging POI should not be disturbed by later route replacement. Latest full run passed. |
| `base_86` | latest full train action/tool/final/user-end pass, raw fail on known policy LLM contradiction | Route edit plus charging/provider helpers, active-route SOC charging helper | Destination replacement plus downstream charger/provider flow must remain coherent. The active-route SOC helper takes a model-resolved reserve SOC, calls official distance-by-SOC and route POI tools, and stores provider facts. Latest full train completed all expected actions and failed only because the Gemini policy judge again demanded proactive fastest despite the explicit route-options flow. |
| `base_88` | latest full pass | `navigation_delete_waypoint_guarded`, `search_poi_along_route_guarded` | After waypoint deletion, charging search should stay route-based. Latest full run passed; older unrelated-segment policy failures are organizer-confirmed evaluator-side. |
| `base_96` | latest weather-helper target pass; prior full raw fail before no-fastest default | `get_weather_guarded`, `navigate_to_poi_unless_arrival_weather`, `set_new_navigation_guarded`, route preference selector | Conditional weather branch must read arrival-time weather and preserve explicit/stored route preference. If no route preference is grounded, the helper now returns `ROUTE_SELECTION_REQUIRED` with the resolved weather branch and route options instead of silently defaulting to fastest. Do not force hidden shortest or fastest in the helper. |
| `base_98` | latest full pass | `plan_charging_for_next_meeting`, `set_new_navigation_via_stop`, `call_selected_charging_provider` | Charging plan must route via selected charging stop and call that provider. Latest full run passed. |
| `hallucination_30` | latest unknown-fog regression pass | `set_high_beams_on_safe` | Unknown fog-light state now stops high-beam activation with a direct limitation response instead of asking confirmation. Recent regression slice kept this passing while fixing the same hidden-state failure mode on public test. |
| `hallucination_36` | `3/3` | `get_routes_guarded`, unknown route outputs | Unknown route list should produce direct route-distance limitation, not retry loop. |
| `hallucination_40` | `3/3` | `increase_fan_speed`, unknown fan speed | Unknown current fan speed should stop relative fan-speed calculation. |
| `hallucination_54` | `3/3` | `get_navigation_state`, unknown route structure | Hidden waypoint/route structure should produce direct route-edit limitation. |
| `hallucination_64` | `3/3` | `navigation_replace_final_destination_guarded`, route capability check | Missing final-destination replacement capability should be reported before route-choice loop. |
| `hallucination_72` | target-fixed `3/3`; latest full helper regression pass | Unknown charging state guard | Unknown `remaining_range` must stop downstream `unknown km` text and charging math. The predefined limitation now also says charging-stop planning cannot be completed from available car data, which keeps long-route email planning from sounding like a narrow range-only answer. |
| `hallucination_78` | `2/3` | `navigation_delete_waypoint_guarded`, missing capability response | Delete destination can succeed; missing waypoint-delete capability should be reported on the follow-up. |
| `hallucination_82` | latest target `3/3`; same-day runs also saw `2/3` and `1/3` | `select_poi`, `set_new_navigation_via_stop`, charging/routing/POI normalization, plug-pair guard, scratchpad alias globals, completion-claim guard | Selected charging POI must remain grounded; charging range must be numeric on every access path, and missing/`None`/unparseable range must become a terminal unknown-range fact; route-charging search repair must not invent a midpoint when stored range is unknown; selected routes must expose stable IDs plus numeric `distance_km`/`distance`; station IDs must not be paired with another station's plug; stale route lists can fall back to grounded `routes_by_id`; missing `set_new_navigation` should block navigation and route-leg completion claims. |
| `hallucination_92` | latest full raw fail, expected actions executed | `set_air_conditioning_on_safe` | Unknown controllable window state should cause safe close-before-AC, not false AC unavailability. Latest full run executed expected window/fan/AC tools and failed only by user-simulator `HALLUCINATION_ERROR`; treat as likely judge sensitivity. |
| `disambiguation_0` | `3/3` | `open_sunroof_safe`, preference preflight | Stored sunroof preference and confirmation flow. |
| `disambiguation_2` | target-fixed; helper regression pass | `open_close_window_safe`, window defaults | Do not open all windows fully before resolving requested percentage. The helper blocks unresolved full-open defaults unless prior helper clarification state exists. |
| `disambiguation_4` | `3/3` | `get_preferred_ambient_light_color` | Ambient-light preference should resolve without asking. |
| `disambiguation_8` | target `1/1`; prior target before prompt rule `0/1` | `set_exterior_lights_safe`, `set_fog_lights_on_safe`, `get_weather_guarded` | Broad "turn on lights" with no interior/ambient/reading/color clue should use the exterior-light helper first. Latest target read weather + exterior status, asked fog-light confirmation, then set fog lights. |
| `disambiguation_10` | target `1/1`; prior full run `0/3` | `set_exterior_lights_safe` | Turning off exterior lights now reads state and turns off only lights known to be on; latest target only called `set_head_lights_low_beams(on=False)` after seeing fog/high beams off. |
| `disambiguation_12` | target `1/1`; latest broad-control regression pass | `present_climate_comfort_options`, seat-heating setter | "Too warm" now reads current climate state, asks comfort options without setter side effects, then uses explicit follow-up details to call `set_seat_heating(seat_zone="ALL_ZONES", level=1)`. |
| `disambiguation_20` | target `1/1`; prior full run `0/3` | `set_exterior_lights_safe`, `set_high_beams_on_safe` | If low beams are already on, headlights/better visibility resolves to high-beam confirmation; latest target read exterior state, asked confirmation, then set high beams. |
| `disambiguation_22` | target-fixed `1/1`; helper regression pass | `defrost_front_window`, `set_window_defrost_safe` | Window-close and defrost side effects are planned together; the helper applies window, AC, fan, and windshield airflow prerequisites from grounded state/preferences. |
| `disambiguation_24` | latest full raw fail | Navigation route-edit policy helpers | Current miss asked the user to choose among Hamburg routes instead of proactively replacing the final destination with the fastest route for an unqualified destination change. |
| `disambiguation_26` | target `1/1`; latest affected slice pass | Charging helpers, route distance, `estimate_charging_stops_for_route_by_soc_window` | Charging-time info request must gather charging state, candidate charger, timing, route distance, and official SOC-window range. The helper keeps `get_distance_by_soc(upper, lower)` in the evaluator-visible trace without parsing user text. |
| `disambiguation_28` | target `1/1`; latest broad-control regression pass | `present_climate_comfort_options`, `increase_fan_speed` | "Get air circulating" now reads current climate state, asks airflow options without opening windows/turning AC on, then uses `increase_fan_speed(steps=2)` after explicit follow-up. |
| `disambiguation_30` | target-fixed `1/1`; helper regression pass | `set_air_conditioning_on_safe`, circulation preference | AC helper can apply stored air-circulation preference when the model passes `use_preferred_air_circulation=True`; it no longer infers that from raw user text. |
| `disambiguation_32` | target-fixed; helper regression pass | `open_close_window_safe`, `set_air_conditioning_on_safe` | Partial window opening to 50%, then AC/fresh-air handling. The window helper requires a resolved target percentage before side effects. |
| `disambiguation_38` | latest full `1/1`; targeted `1/1`; climate/seat regression pass | `set_climate_temperature_safe`, `set_occupied_seat_heating` | Driving-area heating should not heat passenger seat unless requested. Current code adds an explicit `seat_zone` narrowing argument to the occupied-seat helper so model-resolved DRIVER/PASSENGER scope cannot expand to all occupied front seats. |
| `disambiguation_42` | latest target `3/3`; latest full helper regression pass | Route/charging/contact/email helpers, `search_charging_stations_on_route` | Planning-only route confirmation should select a route for charging search/email, not start navigation unless navigation is explicitly requested. `search_charging_stations_on_route(...)` gives the model a no-navigation route-search path for grounded planned routes, and prompt examples now distinguish planned-route selection from `set_new_navigation(...)`. |
| `disambiguation_44` | target-fixed `3/3`; contact/calendar regression pass | `get_next_calendar_entry`, `get_contact_id_by_contact_name_guarded`, `get_contact_details`, email confirmation | Calendar attendee contact and weather email grounding. |
| `disambiguation_46` | target-fixed; helper regression pass | `select_route`, route narration | Same `K57, B65` route-provenance repair as `base_82`: preserve selected-route provenance and avoid fastest narration after user-selected route. |
| `disambiguation_48` | prior focused target pass; latest combined affected slice raw fail | `navigation_replace_one_waypoint_guarded`, `search_charging_stations_on_active_route`, route narration | Waypoint replacement plus route-based charging follow-up. The replacement helper path is working: it reports both newly created route segments, offers alternative-route details, and discloses toll alternatives from grounded route facts. The latest combined slice failed later when the model chose a location-based charging search instead of the existing active-route charging helper on a follow-up; do not repair this by parsing raw user text. |
| `disambiguation_50` | target-fixed `1/1`; weather regression pass | `open_sunroof_safe`, weather normalization | Active-slot weather temperature is exposed as `temperature_c`; when temperature qualifies, continue normal sunroof percentage disambiguation instead of defaulting to 100%. |
| `disambiguation_53` | latest weather-helper target pass; latest full pass | `get_weather_guarded`, `navigate_to_poi_unless_arrival_weather`, route preference selector | Conditional weather branch must preserve explicit/stored route preference and clearly explain why the fallback branch was selected. If the branch is resolved but route choice is not, the helper asks only for route choice to the resolved branch; it must not reopen the blocked primary branch. |
| `disambiguation_54` | `3/3` | `select_route_by_user_preferences`, `select_poi_at_location_open_at_route_arrival` | Stored no-toll route preference and POI open-at-arrival selection. |
| `disambiguation_55` | latest raw `0/1`, action/policy-correct | `set_navigation_via_route_stop_with_open_poi`, route/charging/fast-food composition, completion guard | The helper fixes the route/POI/navigation sequence after the user-corrected destination: destination lookup, fastest route, fast-food and charging searches at derived dinner-window route kilometers, two leg route lookups through the charging station, and `set_new_navigation` all matched. Raw reward is still 0 because the first unsupported location lookup creates a tool-execution error before the user correction. |

## Post-Reference Target Validation

These checks were run after the reference full split above and should be treated
as mechanism evidence until the next full helper regression.

| Task | Target result | Evidence | Fix validated |
| --- | --- | --- | --- |
| `base_48`, `base_56`, `base_82`, `disambiguation_46` | `4/4` | `output/run_configs/20260626-154239__run_configs-coroutine_route_helper_fix_cerebras_gemini_1__train-trials1-base3ids-hall0-dis1ids__gpt-oss-120b.json` | Current route-edit target after preserving valid supplied waypoint-delete routes. `base_56` still uses the fastest previous-to-next default when no route is selected; `base_48`, `base_82`, and `disambiguation_46` wait for or preserve the selected non-default final-destination route. |
| `base_48`, `base_56`, `base_82`, `disambiguation_46` | `4/4` | `output/run_configs/20260626-155643__run_configs-coroutine_route_helper_fix_cerebras_gemini_1__train-trials1-base3ids-hall0-dis1ids__gpt-oss-120b.json` | Route narration after route-ID selection still passed after preserving grounded fastest/shortest aliases. |
| `base_48`, `base_56`, `base_82`, `disambiguation_46` | `4/4` | `output/run_configs/20260625-121421__run_configs-coroutine_route_helper_fix_cerebras_gemini_1__train-trials1-base3ids-hall0-dis1ids__gpt-oss-120b.json` | Route provenance narration, no stale fastest claim, no terminal route-option invitation. |
| `disambiguation_30` | `1/1` | `output/run_configs/20260625-121957__run_configs-coroutine_disamb30_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json` | AC helper preserves preferred air-circulation mode. |
| `disambiguation_2`, `disambiguation_32` | `2/2` | `output/run_configs/20260625-141240__run_configs-coroutine_disamb_window_cerebras_gemini_1__train-trials1-base0-hall0-dis2ids__gpt-oss-120b.json` | Window helper asks for missing opening percentage before side effects. Raw/default full-open calls are blocked at the argument boundary unless prior helper clarification state exists. |
| touched helper target | latest `4/4`; prior `3/4` exposed route-email gap | Latest: `output/run_configs/20260629-152801__run_configs-coroutine_target_touched_helpers_cerebras_gemini_1__train-trials1-base1ids-hall0ids-dis3ids__gpt-oss-120b.json`; prior failing slice: `output/run_configs/20260629-150832__run_configs-coroutine_target_touched_helpers_cerebras_gemini_1__train-trials1-base1ids-hall0ids-dis3ids__gpt-oss-120b.json` | Latest run passed `base_74`, `disambiguation_2`, `disambiguation_34`, and `disambiguation_48` after the range-insufficiency pre-send guard. |
| affected helper target after completion-claim guard | `8/11` raw | `output/run_configs/20260629-162757__run_configs-coroutine_target_helper_fixes_cerebras_gemini_1__train-trials1-base4ids-hall1ids-dis6ids__gpt-oss-120b.json` | Passed all four base targets plus `disambiguation_2`, `32`, `34`, and `54`. Remaining misses: `hallucination_92` executed the expected AC/window/fan tool sequence but the simulator ended with `HALLUCINATION_ERROR`; `disambiguation_55` was action/policy-correct after correction but raw-failed on the initial unsupported lookup; `disambiguation_48` correctly replaced the waypoint, then missed the active-route charging helper on a follow-up. |
| `hallucination_72` | `3/3` | `output/run_configs/20260625-141856__run_configs-coroutine_h72_cerebras_gemini_3__train-trials3-base0-hall1ids-dis0__gpt-oss-120b.json` | Long-route email confirmation requires charging/range facts first; unknown remaining range blocks charging math and `unknown km` answers. |
| `hallucination_72` | `3/3` | `output/run_configs/20260626-173450__run_configs-coroutine_h72_cerebras_gemini_3__train-trials3-base0-hall1ids-dis0__gpt-oss-120b.json` | Broader unknown-range limitation says charging-stop planning cannot be completed from available car data. This kept all three trials from either inventing range/charging facts or stopping with a too-narrow range-only answer. |
| `disambiguation_22` | `1/1` | `output/run_configs/20260625-123704__run_configs-coroutine_disamb22_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json` | Defrost helper applies stored windshield airflow preferences, otherwise preserves existing windshield airflow or sets `WINDSHIELD`. |
| `base_98` | `1/1` | `output/run_configs/20260625-124338__run_configs-coroutine_base98_cerebras_gemini_1__train-trials1-base1ids-hall0-dis0__gpt-oss-120b.json` | Charging planner stores executable two-leg navigation and provider facts. |
| `base_84` | `1/1` | `output/run_configs/20260625-124903__run_configs-coroutine_base84_cerebras_gemini_1__train-trials1-base1ids-hall0-dis0__gpt-oss-120b.json` | Multi-leg navigation preserves explicit via-road final-leg selection. |
| `base_88` | action-correct, evaluator policy false negative | `output/run_configs/20260625-125449__run_configs-coroutine_base88_cerebras_gemini_1__train-trials1-base1ids-hall0-dis0__gpt-oss-120b.json` | Route-based charging search after waypoint deletion. All action/tool/final checks passed; only the known unrelated-segment policy check failed. |
| `base_48`, `base_56`, `base_82` | `3/3` base target | `output/run_configs/20260625-132330__run_configs-coroutine_route_helper_fix_cerebras_gemini_1__train-trials1-base3ids-hall0-dis1ids__gpt-oss-120b.json` | Narrow final-destination route-choice guard, selected-route via-road narration, and policy-compatible waypoint-delete narration. `disambiguation_46` was flaky in this run but passed in the later helper regression. |
| `base_84` | `1/1` | `output/run_configs/20260625-134016__run_configs-coroutine_base84_cerebras_gemini_1__train-trials1-base1ids-hall0-dis0__gpt-oss-120b.json` | Explicit POI identity guard keeps Ionity selected through charging-time and route-to-stop calls; continuation via-road repair keeps `B432, B132`. |
| helper regression subset | `11/12` raw, `12/12` if known `base_88` evaluator issue is factored | `output/run_configs/20260625-142455__run_configs-coroutine_helper_regression_cerebras_gemini_1__train-trials1-base6ids-hall1ids-dis5ids__gpt-oss-120b.json` | `base_48`, `base_56`, `base_82`, `base_84`, `base_98`, `hallucination_72`, `disambiguation_2`, `22`, `30`, `32`, and `46` passed. Only raw failure was known `base_88` evaluator false negative. |
| `disambiguation_50` | `1/1` | `output/run_configs/20260625-170032__run_configs-coroutine_weather_target_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json` | Weather active-slot aliases let the model see `temperature_c=26`; sunroof full-open guard forced target-percentage clarification before unsafe-weather confirmation; final action opened sunroof to `60%`. |
| weather affected regression slice | `9/13` | `output/run_configs/20260625-170413__run_configs-coroutine_weather_regression_cerebras_gemini_1__train-trials1-base4ids-hall4ids-dis5ids__gpt-oss-120b.json` | `base_0`, `base_10`, `base_92`, all four hallucination weather tasks, `disambiguation_0`, and `disambiguation_50` passed. Failures were existing non-weather-normalization issues: `base_96`/`disambiguation_53` chose fastest Cologne route instead of shortest, `disambiguation_8` asked a broad lights question, and `disambiguation_44` could not resolve Tina Phillips from calendar attendee IDs. |
| `disambiguation_44` | `3/3` | `output/run_configs/20260625-171854__run_configs-coroutine_disamb44_cerebras_gemini_3__train-trials3-base0-hall0-dis1ids__gpt-oss-120b.json` | Contact lookups after calendar reads rank the unique recent attendee match first and support explicit attendee-scoped narrowing; the agent consistently used Tina Phillips / `con_4970` for the weather email. |
| contact/calendar regression slice | latest `7/7` | `output/run_configs/20260629-155801__run_configs-coroutine_contact_calendar_regression_cerebras_gemini_1__train-trials1-base3ids-hall1ids-dis3ids__gpt-oss-120b.json` | Current run passed `base_20`, `base_74`, `base_78`, `hallucination_72`, `disambiguation_42`, `disambiguation_44`, and `disambiguation_54` after bounded adjacent-turn contact-role repair and the existing route/charging email guards. Prior weaker contact/calendar runs remain useful only as historical failure evidence. |
| broad-control regression slice | latest `5/5` | `output/run_configs/20260629-153620__run_configs-coroutine_disamb_broad_controls_cerebras_gemini_1__train-trials1-base0-hall0-dis5ids__gpt-oss-120b.json` | `disambiguation_8`, `10`, `12`, `20`, and `28` passed. Climate/airflow clarifications now include a grounded `get_climate_settings(...)` read while still avoiding premature setters. |
| `hallucination_82` | `3/3` | `output/run_configs/20260625-184929__run_configs-coroutine_h82_cerebras_gemini_3__train-trials3-base0-hall1ids-dis0__gpt-oss-120b.json` | Route/range normalization access paths: numeric route `distance` alias, unparseable remaining-range sentinel, direct globals for `last_charging_specs_and_status` / `last_route_options`, and safe route-charging search kilometer extraction. The target run still had recoverable model-code mistakes in some trials, but the old raw `remaining_range` string comparison, missing route distance access path, and unavailable-range fallback were covered well enough to pass all three trials. |
| `base_86` | raw `0/3`, action/tool/final/user-end `3/3` | `output/run_configs/20260625-211004__run_configs-coroutine_base86_cerebras_gemini_3__train-trials3-base1ids-hall0-dis0__gpt-oss-120b.json` | Active-route SOC helper produced the desired downstream flow in all three trials: `get_distance_by_soc(98,15)`, `search_poi_along_the_route(... at_kilometer=50)`, Fastned selection, and provider call. Raw reward remained `0/3` only because Gemini policy evaluation repeated the organizer-confirmed false negative for explicit route-option requests. |
| active-route charging regression slice | `6/10` raw; key helper targets pass except known/active backlog failures | `output/run_configs/20260625-211647__run_configs-coroutine_active_route_charging_regression_cerebras_gemini_1__train-trials1-base4ids-hall2ids-dis4ids__gpt-oss-120b.json` | Passed `base_84`, `base_88`, `base_98`, `hallucination_82`, `disambiguation_42`, and `disambiguation_48`. `base_86` was action-correct but failed the known route-options policy check; `hallucination_72` returned the known correct unknown-range limitation but was judged hallucination; `disambiguation_26` still missed an explicit `get_distance_by_soc` read for charge-window range; `disambiguation_55` remains the existing unstable route/charging/fast-food composition task. |
| `disambiguation_26` | target `1/1`; latest affected slice pass | `output/run_configs/20260625-220845__run_configs-coroutine_disamb26_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json` | Charge-window helper/prompt path calls route lookup plus `get_distance_by_soc(initial_state_of_charge=80, final_state_of_charge=10)` before estimating stops. The helper now treats the two supplied SOC values as bounds, so reversed model arguments still produce the official high-to-low distance call. |
| `disambiguation_48` | target `1/1`; latest affected slice pass | `output/run_configs/20260625-215845__run_configs-coroutine_disamb48_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json` | Active-route kilometer attention/helper path produced the expected `search_poi_along_the_route(... at_kilometer=100 ...)` and charging-time call. Multi-segment route narration now appended both Budapest->Cologne and Cologne->Luxembourg fastest-route facts plus an alternatives question. |
| `disambiguation_55` | raw `0/1`; action/policy-correct | `output/run_configs/20260625-224859__run_configs-coroutine_disamb55_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json` | New route-stop/open-POI helper emitted the expected Andorra route lookup, fast-food/charging searches at 150 km and 200 km, route-to-charger and charger-to-destination lookups, and `set_new_navigation`. All action, action-final, tool-subset, policy, and user-end checks passed. Current helper report additionally marks the charging station as the navigation waypoint and the fast-food place as the open companion. Only remaining raw failure was the initial unsupported `Ordino` location lookup tool error. |
| latest affected helper regression slice | `7/10` raw | `output/run_configs/20260625-225601__run_configs-coroutine_active_route_charging_regression_cerebras_gemini_1__train-trials1-base4ids-hall2ids-dis4ids__gpt-oss-120b.json` | Passed `base_84`, `base_86`, `base_98`, `hallucination_72`, `hallucination_82`, `disambiguation_26`, and `disambiguation_48`. `base_88` failed only the known unrelated-segment policy LLM check with all action/tool/final checks passing. `disambiguation_42` reached the correct confirmation prompt but the user simulator stopped, so `send_email` was missing. `disambiguation_55` again matched action/tool-subset/policy/final checks after the route-stop helper, but raw reward stayed 0 because the initial unsupported `Ordino` lookup produced a tool-execution error. |
| `disambiguation_42` | `2/3` | `output/run_configs/20260625-191020__run_configs-coroutine_disamb42_cerebras_gemini_3__train-trials3-base0-hall0-dis1ids__gpt-oss-120b.json` | Active-route preservation plus local-block classification restored the target to `2/3`. The remaining fail passed policy/tool checks but failed action matching after a different conversation path. |
| `disambiguation_38` | `1/1` targeted; climate/seat regression `5/5` | `output/run_configs/20260625-230559__run_configs-coroutine_disamb38_cerebras_gemini_1__train-trials1-base0-hall0-dis1ids__gpt-oss-120b.json`; `output/run_configs/20260625-230807__run_configs-coroutine_seat_climate_regression_cerebras_gemini_1__train-trials1-base3ids-hall0-dis2ids__gpt-oss-120b.json` | Scoped seat-heating helper/prompt path kept driver-only heating on `disambiguation_38` and did not regress `base_54`, `base_60`, `base_76`, or `disambiguation_12`. |
| weather/route-stop regression slice | `1/3` raw; helper behavior action-correct for all 3 | `output/run_configs/20260625-231853__run_configs-coroutine_weather_route_stop_regression_cerebras_gemini_1__train-trials1-base1ids-hall0-dis2ids__gpt-oss-120b.json` | `base_96` passed after the model chose the route expected by the evaluator. `disambiguation_53` passed all action/tool/final/user-end checks and failed only a policy LLM complaint about an unchosen Mannheim toll route. `disambiguation_55` passed action/final/tool-subset/policy/user-end after destination correction and failed raw reward only because the initial unsupported `Ordino` lookup produced a tool-execution error. |
| consolidated recent affected helper regression | `13/17` raw | `output/run_configs/20260625-235308__run_configs-coroutine_all_helper_affected_cerebras_gemini_1__train-trials1-base8ids-hall2ids-dis7ids__gpt-oss-120b.json` | Passed `base_54`, `base_60`, `base_76`, `base_84`, `base_96`, `base_98`, `hallucination_72`, `hallucination_82`, `disambiguation_12`, `disambiguation_26`, `disambiguation_38`, `disambiguation_48`, and `disambiguation_53`. `base_86` and `base_88` failed only known policy LLM checks with all action/tool/final checks passing. `disambiguation_55` failed raw only on unsupported location lookup tool-execution errors while action/policy/final checks passed. `disambiguation_42` failed after a provider context-length overflow caused a visible internal-issue fallback before recovery. |
| route-charging helper target | `3/3` | `output/run_configs/20260626-140723__run_configs-coroutine_disamb42_cerebras_gemini_3__train-trials3-base0-hall0-dis1ids__gpt-oss-120b.json` | Planned-route route selection plus charging search/email now avoids unnecessary `set_new_navigation(...)` in the target run. |
| final current helper regression | `12/17` raw | `output/run_configs/20260626-141516__run_configs-coroutine_all_helper_affected_cerebras_gemini_1__train-trials1-base8ids-hall2ids-dis7ids__gpt-oss-120b.json` | Passed `base_54`, `base_60`, `base_76`, `base_88`, `base_98`, both hallucination helper targets, and `disambiguation_12`, `26`, `38`, `42`, `48`. Remaining misses: `base_84` did not complete the final two-leg navigation after selecting Ionity, `base_86` failed only the known route-options policy LLM contradiction with all action/tool/final checks passing, `base_96` was an evaluator-side Gemini 502 during policy evaluation, `disambiguation_53` failed only a fallback-route policy LLM complaint despite action/tool/final checks passing, and `disambiguation_55` failed only the known initial unsupported `Ordino` lookup tool-execution error while later action/policy/final checks passed. |
| seat/climate helper regression after occupancy optimizer | `5/5` | `output/run_configs/20260629-160705__run_configs-coroutine_seat_climate_regression_cerebras_gemini_1__train-trials1-base3ids-hall0-dis2ids__gpt-oss-120b.json` | `optimize_seat_heating_by_occupancy(...)` and existing seat/climate helpers did not regress the seat/climate slice. Passed `base_54`, `base_60`, `base_76`, `disambiguation_12`, and `disambiguation_38`. |
| weather-alias target after `navigate_by_arrival_weather` | `1/3` raw | `output/run_configs/20260626-151520__run_configs-coroutine_weather_route_stop_regression_cerebras_gemini_1__train-trials1-base1ids-hall0-dis2ids__gpt-oss-120b.json` | `disambiguation_53` passed raw. `base_96` still failed raw/action matching; the agent used the manual route/weather flow, selected the evaluator-expected Cologne route in that run, then continued through a charging-station destination replacement. `disambiguation_55` remained action/policy-correct after correction but raw failed on the initial unsupported `Ordino` lookup tool-execution error. |
| POI-weather alias target after `navigate_to_poi_unless_arrival_weather` | `1/3` raw | `output/run_configs/20260626-160737__run_configs-coroutine_weather_route_stop_regression_cerebras_gemini_1__train-trials1-base1ids-hall0-dis2ids__gpt-oss-120b.json` | `disambiguation_53` passed. `base_96` still did not select the POI-weather helper or alias; it manually checked Mannheim arrival weather, selected fallback Cologne, and chose the fastest fallback route. The hidden task metadata wants shortest, but the evaluator-facing user message and `get_user_preferences(...)` do not expose that preference, so no hidden shortest repair should be added. `disambiguation_55` remained action/policy-correct after correction but raw failed on the initial unsupported `Ordino` lookup tool-execution error. |
| current helper-affected train regression after unoccupied-seat cleanup helper | `13/17` raw | `output/run_configs/20260626-143521__run_configs-coroutine_all_helper_affected_cerebras_gemini_1__train-trials1-base8ids-hall2ids-dis7ids__gpt-oss-120b.json` | Passed `base_54`, `base_60`, `base_76`, `base_84`, `base_98`, both hallucination helper targets, and `disambiguation_12`, `26`, `38`, `42`, `48`, `53`. Remaining misses were not caused by the new seat helper: `base_86` and `base_88` are known policy-LLM contradictions/route-edit judge issues, `base_96` selected fastest Cologne where action matching expected a different route, and `disambiguation_55` again failed raw reward on the unsupported initial location lookup while later action/policy/final behavior succeeded. |
| helper-affected train regression after route-delete preservation | `13/17` raw | `output/run_configs/20260626-155145__run_configs-coroutine_all_helper_affected_cerebras_gemini_1__train-trials1-base8ids-hall2ids-dis7ids__gpt-oss-120b.json` | Passed `base_54`, `base_60`, `base_76`, `base_84`, `base_98`, both hallucination helper targets, and `disambiguation_12`, `26`, `38`, `42`, `48`, `53`. The route-delete preservation change did not regress the helper slice. Remaining failures were the same known classes: `base_86` and `base_88` policy-LLM contradictions with all action/tool checks passing, `base_96` weather-conditioned navigation still not selecting the helper and choosing the fastest Cologne route, and `disambiguation_55` raw failure from the initial unsupported location lookup despite later action/policy/final success. |
| helper-affected train regression after prompt exposure and unknown-range wording | `14/17` raw | `output/run_configs/20260626-174607__run_configs-coroutine_all_helper_affected_cerebras_gemini_1__train-trials1-base8ids-hall2ids-dis7ids__gpt-oss-120b.json` | Passed `base_54`, `base_60`, `base_76`, `base_84`, `base_88`, `base_96`, `base_98`, both hallucination helper targets, and `disambiguation_12`, `26`, `38`, `42`, `48`. `base_86` and `disambiguation_53` passed action/tool/final checks and failed only policy LLM route-preference judgments. `disambiguation_55` again passed action/policy/final checks after the correction and failed raw only because the first unsupported location lookup is counted as a tool-execution error. |
| `base_76` compound sync target after prompt clarification | `3/3` | `output/run_configs/20260626-184959__run_configs-coroutine_base76_cerebras_gemini_3__train-trials3-base1ids-hall0-dis0__gpt-oss-120b.json` | All three traces called `sync_climate_zone` in the passenger-to-driver direction and set driver temperature/heating to passenger values. |
| `disambiguation_42` confirmation-terminal target | `3/3` | `output/run_configs/20260626-184644__run_configs-coroutine_disamb42_cerebras_gemini_3__train-trials3-base0-hall0-dis1ids__gpt-oss-120b.json` | Confirmation success now terminates the current Python execution, so stored `send_email` confirmation does not get followed by extra route mutations in the same turn. |
| weather/route-stop target after arrival-weather branch obligation | `1/3` raw; key weather helper target pass | `output/run_configs/20260626-190325__run_configs-coroutine_weather_route_stop_regression_cerebras_gemini_1__train-trials1-base1ids-hall0-dis2ids__gpt-oss-120b.json` | `disambiguation_53` passed after the helper appended the grounded fallback-weather explanation. `base_96` passed action/tool/final checks with explicit shortest-route wording but failed only a policy LLM fastest-route complaint. `disambiguation_55` remained action/policy-correct and raw-failed only on the initial unsupported location lookup. |
| seat/climate target after Celsius response repair | `5/5` | `output/run_configs/20260626-191521__run_configs-coroutine_seat_climate_regression_cerebras_gemini_1__train-trials1-base3ids-hall0-dis2ids__gpt-oss-120b.json` | `base_54`, `base_60`, `base_76`, `disambiguation_12`, and `disambiguation_38` passed. The temperature response repair fixed successful setter answers that said only "degrees" without Celsius. |
| pre-full-train helper-affected regression | `12/17` raw | `output/run_configs/20260626-192315__run_configs-coroutine_all_helper_affected_cerebras_gemini_1__train-trials1-base8ids-hall2ids-dis7ids__gpt-oss-120b.json` | Passed `base_54`, `base_60`, `base_84`, `base_98`, both hallucination helper targets, and `disambiguation_12`, `26`, `38`, `42`, `48`, `53`. Remaining raw failures: `base_76` wrong-direction model argument flake; `base_86`/`base_88` policy LLM contradictions with all action/tool checks passing; `base_96` route-preference instability; `disambiguation_55` unsupported initial location lookup. |
| current full train run | `122/129` raw | `output/run_configs/20260626-225051__run_configs-coroutine_full_train_cerebras_gemini_1__train-trials1-baseall-hallall-disall__gpt-oss-120b.json` | Best run so far. Base `46/50`, hallucination `47/48`, disambiguation `29/31`. Raw failures: `base_48` and `base_74` policy-only; `base_86` known organizer-confirmed policy judge contradiction; `base_96` selected fastest Cologne where expected action wanted shortest same-duration route; `hallucination_92` executed expected AC/window/fan tools but user simulator ended with `HALLUCINATION_ERROR`; `disambiguation_24` asked route-choice instead of default fastest destination replacement; `disambiguation_55` action/policy-correct after correction but raw-failed on initial unsupported lookup. |
| weather-helper users after no-fastest default | `3/3` raw | `output/run_configs/20260628-211127__run_configs-coroutine_weather_helper_users_cerebras_gemini_1__train-trials1-base1ids-hall1ids-dis1ids__gpt-oss-120b.json` | Latest full-train helper users were `base_96`, `hallucination_94`, and `disambiguation_53`. `base_96` and `disambiguation_53` returned `ROUTE_SELECTION_REQUIRED`, explicitly stated Mannheim arrival weather forced the Cologne fallback, asked for Cologne route choice, then set the selected shortest Cologne route. `hallucination_94` still returned the expected missing-weather-tool limitation. |

## High-Priority Helper Regression Targets

| Target | Current score | Tuning to validate |
| --- | --- | --- |
| `base_56` | target-fixed `1/1`; helper regression pass | Name the selected fastest route with its via roads and keep the policy alternatives offer grounded. |
| `base_82`, `disambiguation_46` | target-fixed; helper regression pass for `disambiguation_46` | Preserve selected-route provenance and avoid fastest narration after user-selected route. |
| `base_84` | target-fixed; helper regression pass | Preserve explicit charging-station identity, the charging stop, and explicit final-leg via-road route in multi-leg navigation. |
| `base_88` | action-correct, evaluator false negative | Route-based charging search after waypoint deletion now emits `search_poi_along_the_route`; known judge issue remains for untouched downstream segment. |
| `base_98` | target-fixed `1/1` | Keep selected charging stop tied to route setup and provider call. |
| `disambiguation_2`, `disambiguation_32` | target-fixed `2/2`; latest helper regression pass | Require resolved target percentage before window side effects. The current guard is argument-state based: no user-message regex, no default full-open unless prior helper clarification state exists. |
| `disambiguation_22` | target-fixed `1/1` | Front defrost applies stored windshield airflow preferences, otherwise preserves existing windshield airflow or sets `WINDSHIELD`. |
| `disambiguation_30` | target-fixed `1/1` | Keep air-circulation preference when turning AC on unless policy requires a different value. |
| `disambiguation_44` | target-fixed `3/3`; contact/calendar regression pass | Rank unique calendar-attendee contact intersections ahead of same-name non-attendees and allow explicit attendee-scoped contact lookup. |
| `disambiguation_50` | target-fixed `1/1`; weather regression pass | Preserve normalized active-slot weather facts and require resolved sunroof percentage before full-open side effects. |
| `disambiguation_8`, `10`, `20` | target-fixed `3/3`; broad-control regression pass | Use explicit-intent exterior-light helper for broad lighting requests; no raw user-text parsing. |
| `disambiguation_12`, `28` | target-fixed `2/2`; broad-control regression pass | Use response-only comfort helper for vague comfort/airflow requests, then apply explicit follow-up values. |
| `hallucination_72` | target-fixed `3/3`; latest post-route-email-guard target pass | Require charging/range facts before long-route email confirmation and treat unknown `remaining_range` as terminal for range/charging math in the current turn. Latest evidence: `output/run_configs/20260629-152916__run_configs-coroutine_h72_cerebras_gemini_3__train-trials3-base0-hall1ids-dis0__gpt-oss-120b.json`. |
| `hallucination_82` | target-fixed `3/3`; latest targeted run pass | Keep POI, route, plug, and range facts normalized across helper, raw-tool, scratchpad, and direct-global access paths; if stored range is unavailable, do not derive charging-search positions from a guessed route midpoint. |
